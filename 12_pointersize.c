//pointer size
#include<stdio.h>
void main()
{
  int *ptr;
  char *ptch;
  float *ptf;
  double *ptd;
  printf("size of int type ptr : %d\n",sizeof(ptr));
  printf("size of char type ptr : %d\n",sizeof(ptch));
  printf("size of float type ptr : %d\n",sizeof(ptf));
  printf("size of double type ptr : %d\n",sizeof(ptd));
}